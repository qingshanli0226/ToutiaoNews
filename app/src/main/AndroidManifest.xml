<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.toutiaonews">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".App"
        tools:replace="android:name"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true">
        <activity android:name=".fragment_mine.activity.SettingActivity" />
        <activity android:name=".fragment_mine.activity.FeedbackActivity" />
        <activity android:name=".fragment_mine.activity.DiscloseActivity" />
        <activity android:name=".fragment_mine.activity.JDongActivity" />
        <activity android:name=".fragment_mine.activity.StoreActivity" />
        <activity android:name=".fragment_mine.activity.InformActivity" />
        <activity android:name=".fragment_mine.activity.HistoryActivity" />
        <activity android:name=".fragment_mine.activity.CollectActivity" />
        <activity android:name=".fragment_mine.activity.SevenActivity" />
        <activity android:name=".fragment_mine.activity.FansActivity" />
        <activity android:name=".fragment_mine.activity.Action1Activity" />
        <activity
            android:name=".fragment_mine.activity.RegisterActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask"
            android:theme="@style/TranslucentTheme"

            android:windowIsTranslucent="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="design_width_in_dp"
            android:value="360" />
        <meta-data
            android:name="design_height_in_dp"
            android:value="640" />
    </application>

</manifest>